{
  "//": [
    "The single source of truth for what riprap wires. Both riprap's CI and",
    "bin/riprap verify read this file, so the checker cannot drift from the wiring.",
    "",
    "Every command here runs from the plugin, but only session-start does its work",
    "there. The guardrail hooks are dispatched by run-payload-hook, which execs the",
    "real script out of the project at bin/hooks/riprap/claude/ — and exits 0 in",
    "silence if the project never ran /riprap:install, so this plugin is inert in",
    "repositories that do not use it.",
    "",
    "The guardrail scripts live in the project rather than here, and that is",
    "deliberate. They share pattern libraries with the git hooks, which run for",
    "every developer on the team — including ones who never installed this plugin.",
    "A library living in a version-stamped, user-scoped plugin cache would simply",
    "be missing for half of them. One rule definition, two enforcers, both",
    "reachable from a plain clone.",
    "",
    "lint-example.sh is deliberately absent: it is an inert template, and wiring a",
    "rule that never fires teaches people to ignore the wiring.",
    "",
    "No single event covers every moment the handoff rule cares about, so it is",
    "wired to several: PreCompact is the only warning that context is about to be",
    "lost, and it is the one event that cannot say anything to the model — it can",
    "only block, and blocking a compaction that is already necessary wedges the",
    "session. So it writes to disk, and SessionStart on `compact` is what points",
    "the next context at what it wrote. SessionEnd does the same, as a last-resort",
    "net for a session that never went through PreCompact at all. PostToolUse on",
    "ExitPlanMode asks for the first handoff once a plan exists to write one from,",
    "and Stop asks again whenever the tree has moved past what was last written.",
    "",
    "One of these blocks rather than asks: PreToolUse on ScheduleWakeup, CronCreate,",
    "Workflow, RemoteTrigger and a backgrounded Agent/Task refuses the call when no",
    "handoff exists yet for the branch -- except a ScheduleWakeup that is itself ending",
    "a loop, or a foreground Agent/Task dispatch, neither of which is unattended in the",
    "sense that matters here. Every other handoff hook only asks, because nothing else",
    "here can compel a model to write anything -- but a session about to become",
    "unattended cannot be asked again once it is, so this is the one case where",
    "refusing the call is the only mechanism that can still work.",
    "",
    "SessionStart matches `resume` and `fork` as well. Those are not new features —",
    "they are two of the five documented matcher values, and omitting them meant a",
    "resumed session received no router at all, which is the single moment a handoff",
    "is most worth surfacing."
  ],
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact|fork",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/session-start",
            "shell": "bash",
            "async": false
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          { "type": "command", "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/run-payload-hook block-destructive-outside-cwd.sh" },
          { "type": "command", "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/run-payload-hook block-unreviewed-merge.sh" }
        ]
      },
      {
        "matcher": "ExitPlanMode",
        "hooks": [
          { "type": "command", "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/run-payload-hook require-plan-stress-test.sh" }
        ]
      },
      {
        "matcher": "ScheduleWakeup|CronCreate|Workflow|RemoteTrigger|Agent|Task",
        "hooks": [
          { "type": "command", "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/run-payload-hook require-handoff-before-unattended.sh" }
        ]
      },
      {
        "matcher": "Bash|Read|Grep|Edit|Write",
        "hooks": [
          { "type": "command", "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/run-payload-hook lint-secrets.sh" }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          { "type": "command", "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/run-payload-hook lint-tech-footprint.sh" }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          { "type": "command", "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/run-payload-hook format-on-write.sh" }
        ]
      },
      {
        "matcher": "ExitPlanMode",
        "hooks": [
          { "type": "command", "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/run-payload-hook handoff-plan-approved.sh" }
        ]
      }
    ],
    "PreCompact": [
      {
        "hooks": [
          { "type": "command", "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/run-payload-hook handoff-precompact.sh" }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          { "type": "command", "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/run-payload-hook handoff-stop.sh", "timeout": 5 }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          { "type": "command", "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/run-payload-hook session-end.sh" }
        ]
      }
    ]
  }
}
